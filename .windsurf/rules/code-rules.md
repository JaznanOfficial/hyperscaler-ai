---
trigger: always_on
---

don't write too much code in a single file.
always use multiple files, reusable small components.
use same patterns for same kind of operations.
in this project, we'll use shadcn, react-query, zod, tailwindcss, nextjs, typescript, sonner, better-auth, neondb, drizzle etc. don't use other libraries or frameworks for the same purpose, you always use the same library or framework for the same purpose. 
for shadcn, nextjs use their mcp and for all packages or frameworks use their latest stable version.
always use bun as your package manager. never use npm or yarn or pnpm or any other package manager.